import fs from 'node:fs';
import assert from 'node:assert/strict';
const index=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const app=fs.readFileSync(new URL('../app.js',import.meta.url),'utf8');
const css=fs.readFileSync(new URL('../experience.css',import.meta.url),'utf8');
const manifest=JSON.parse(fs.readFileSync(new URL('../manifest.json',import.meta.url),'utf8'));
for(const token of ['capability-hero','hero-copy','input-stack','center-stage','funnel-rim','authority-orb','platform','output-stack','summary-bar'])assert.match(index,new RegExp(`class="[^"]*${token}`));
assert.match(index,/Questions that expose the real enablement system/);assert.match(index,/THE REASONABLE QUESTION/);assert.match(index,/Workflow design &amp; automation/);assert.doesNotMatch(index,/transformer-svg|class="coil"|class="pulse"/);
assert.match(app,/getContext\('webgl2'/);assert.match(app,/class FunnelScene/);assert.match(app,/this\.surface=makeProgram/);assert.match(app,/this\.mesh=makeProgram/);assert.match(app,/this\.particles=makeProgram/);assert.match(app,/vec3 learn\(/);assert.match(app,/if\(this\.visible\)this\.raf=requestAnimationFrame\(tick\)/);assert.match(app,/visibilitychange/);assert.match(app,/reduced\.matches/);assert.match(app,/function applyScenario\(key/);assert.match(app,/setAttribute\('aria-pressed'/);assert.match(app,/resetScenario/);
assert.match(css,/\.center-stage\{[^}]*left:35%[^}]*right:24\.5%/);assert.match(css,/@media\(max-width:1320px\)/);assert.match(css,/\.experience-home #plan\{background:#fff/);assert.match(css,/@media\(max-width:980px\)/);assert.match(css,/@media\(max-width:680px\)/);assert.match(css,/@media\(max-width:360px\)/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);
for(const path of ['index.html','experience.css','app.js','qa/capability-transformer.contract.mjs'])assert.ok(manifest.files.includes(path),`manifest missing ${path}`);
assert.doesNotMatch([index,app,css].join('\n'),/role[\s_-]*forge/i);
console.log('Capability Transformer continuous-motion and geometry contract passed.');
